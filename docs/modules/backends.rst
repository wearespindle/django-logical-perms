.. _backends_module:

``backends`` module
===================

.. note::
    The custom Django authentication backend can be used to integrate logical permissions with Django's built-in
    authentication and permissions framework. You can read more about it :ref:`here <integrating_django>`.

Provides the Django integration backend implementation.

.. automodule:: django_logical_perms.backends
    :members: