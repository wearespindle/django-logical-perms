.. _storages_module:

``storages`` module
===================

Provides the storage facility for logical permissions. Its ``default_storage`` is used to
register permissions so that they can be used with Django's built-in ``user.has_perm()``.

.. automodule:: django_logical_perms.storages
    :members: